apiVersion: v1
kind: ResourceQuota
metadata:
  name: checkout-budget
  annotations:
    argocd.argoproj.io/sync-wave: '-1'
spec:
  hard:
    requests.cpu: 500m
    limits.cpu: '1'
    requests.memory: 256Mi
    limits.memory: 512Mi
    pods: '4'
